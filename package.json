{
  "name": "loft-cypress-automation",
  "version": "1.0.0",
  "description": "Loft.ph Cypress Automation Scripts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress": "npx cypress run",
    "cypress-open": "npx cypress open", 
    "cypress-chrome": "npx cypress run --browser chrome --headed",
    "cypress-edge": "npx cypress run --browser edge --headed",
    "cypress-firefox": "npx cypress run --browser firefox --headed"
  },
  "author": "Reander Agulto",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.1"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
